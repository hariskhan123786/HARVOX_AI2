# Harvox Desktop Agent

Run `start-agent.cmd` while using Harvox on this Windows PC. The agent listens only on `127.0.0.1`, verifies your Harvox login token, and executes a small approved action set. Close its terminal window to stop it.
