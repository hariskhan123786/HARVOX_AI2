import "./chunk-LPCC3CIV.js";
import {
  getLinkColor,
  getRandom,
  getRangeValue,
  getStyleFromRgb,
  half,
  originPoint,
  rangeColorToRgb
} from "./chunk-2FJEDR7Q.js";
import {
  __publicField
} from "./chunk-2TUXWMP5.js";

// node_modules/@tsparticles/interaction-particles-links/browser/Utils.js
function getLinkKey(ids) {
  return [...ids].sort((a, b) => a - b).join("_");
}
function setLinkFrequency(particles, dictionary) {
  const key = getLinkKey(particles.map((t) => t.id));
  let res = dictionary.get(key);
  if (res === void 0) {
    res = getRandom();
    dictionary.set(key, res);
  }
  return res;
}

// node_modules/@tsparticles/interaction-particles-links/browser/LinkInstance.js
var minOpacity = 0;
var minWidth = 0;
var minDistance = 0;
var maxFrequency = 1;
var defaultFrequency = 0;
var LinkInstance = class {
  constructor(pluginManager, container) {
    __publicField(this, "_colorCache", /* @__PURE__ */ new Map());
    __publicField(this, "_container");
    __publicField(this, "_freqs");
    __publicField(this, "_pluginManager");
    this._pluginManager = pluginManager;
    this._container = container;
    this._freqs = { links: /* @__PURE__ */ new Map(), triangles: /* @__PURE__ */ new Map() };
  }
  drawParticle(context, particle) {
    var _a;
    const { links, options } = particle;
    if (!(links == null ? void 0 : links.length) || !options.links) {
      return;
    }
    const linkOpts = options.links, width = particle.retina.linksWidth ?? minWidth, pos1 = particle.getPosition(), twinkle = (_a = particle.options["twinkle"]) == null ? void 0 : _a.links, trianglesEnabled = linkOpts.triangles.enable, p1Destinations = trianglesEnabled ? new Set(links.map((l) => l.destination.id)) : null, originalAlpha = context.globalAlpha;
    let currentColorStyle = "", currentWidth = -1, currentAlpha = -1, pathOpen = false;
    const flushLines = () => {
      if (pathOpen) {
        context.stroke();
        pathOpen = false;
      }
    };
    for (const link of links) {
      if (linkOpts.frequency < maxFrequency && this._getLinkFrequency(particle, link.destination) > linkOpts.frequency) {
        continue;
      }
      const pos2 = link.destination.getPosition();
      if (trianglesEnabled && !link.isWarped && p1Destinations) {
        flushLines();
        this._drawTriangles(options, particle, link, p1Destinations, pos1, pos2, context);
      }
      if (link.opacity <= minOpacity || width <= minWidth) {
        continue;
      }
      if (!linkOpts.enable) {
        continue;
      }
      let opacity = link.opacity, colorLine = link.color;
      const twinkleRgb = (twinkle == null ? void 0 : twinkle.enable) && getRandom() < twinkle.frequency ? rangeColorToRgb(this._pluginManager, twinkle.color) : void 0;
      if (twinkle && twinkleRgb) {
        colorLine = twinkleRgb;
        opacity = getRangeValue(twinkle.opacity);
      }
      if (!colorLine) {
        const linkColor = linkOpts.id !== void 0 ? this._container.particles.linksColors.get(linkOpts.id) : this._container.particles.linksColor;
        colorLine = getLinkColor(particle, link.destination, linkColor);
      }
      if (!colorLine) {
        continue;
      }
      const colorStyle = this._getCachedStyle(colorLine);
      if (colorStyle !== currentColorStyle || width !== currentWidth || opacity !== currentAlpha) {
        flushLines();
        context.strokeStyle = colorStyle;
        context.lineWidth = width;
        context.globalAlpha = opacity;
        currentColorStyle = colorStyle;
        currentWidth = width;
        currentAlpha = opacity;
        context.beginPath();
        pathOpen = true;
      }
      if (link.isWarped) {
        const canvasSize = this._container.canvas.size, dx = pos2.x - pos1.x, dy = pos2.y - pos1.y;
        let sx = originPoint.x, sy = originPoint.y;
        if (Math.abs(dx) > canvasSize.width * half) {
          sx = dx > minDistance ? -canvasSize.width : canvasSize.width;
        }
        if (Math.abs(dy) > canvasSize.height * half) {
          sy = dy > minDistance ? -canvasSize.height : canvasSize.height;
        }
        context.moveTo(pos1.x, pos1.y);
        context.lineTo(pos2.x + sx, pos2.y + sy);
        context.moveTo(pos1.x - sx, pos1.y - sy);
        context.lineTo(pos2.x, pos2.y);
      } else {
        context.moveTo(pos1.x, pos1.y);
        context.lineTo(pos2.x, pos2.y);
      }
    }
    flushLines();
    context.globalAlpha = originalAlpha;
  }
  init() {
    this._freqs.links.clear();
    this._freqs.triangles.clear();
    this._colorCache.clear();
    return Promise.resolve();
  }
  particleCreated(particle) {
    particle.links = [];
    if (!particle.options.links) {
      return;
    }
    particle.linksDistance = particle.options.links.distance;
    particle.linksWidth = particle.options.links.width;
    const ratio = this._container.retina.pixelRatio;
    particle.retina.linksDistance = particle.linksDistance * ratio;
    particle.retina.linksWidth = particle.linksWidth * ratio;
  }
  particleDestroyed(particle) {
    particle.links = [];
  }
  _drawTriangles(options, p1, link, p1Destinations, pos1, pos2, context) {
    var _a, _b, _c;
    const p2 = link.destination, triangleOptions = (_a = options.links) == null ? void 0 : _a.triangles;
    if (!(triangleOptions == null ? void 0 : triangleOptions.enable) || !((_b = p2.options.links) == null ? void 0 : _b.triangles.enable)) {
      return;
    }
    const p2Links = p2.links;
    if (!(p2Links == null ? void 0 : p2Links.length)) {
      return;
    }
    for (const vertex of p2Links) {
      if (vertex.isWarped || this._getLinkFrequency(p2, vertex.destination) > p2.options.links.frequency || !p1Destinations.has(vertex.destination.id)) {
        continue;
      }
      const p3 = vertex.destination;
      if (this._getTriangleFrequency(p1, p2, p3) > (((_c = options.links) == null ? void 0 : _c.triangles.frequency) ?? defaultFrequency)) {
        continue;
      }
      const opacityTriangle = triangleOptions.opacity ?? (link.opacity + vertex.opacity) * half, colorTriangle = rangeColorToRgb(this._pluginManager, triangleOptions.color) ?? link.color;
      if (!colorTriangle || opacityTriangle <= minOpacity) {
        continue;
      }
      const pos3 = p3.getPosition();
      context.save();
      context.fillStyle = this._getCachedStyle(colorTriangle);
      context.globalAlpha = opacityTriangle;
      context.beginPath();
      context.moveTo(pos1.x, pos1.y);
      context.lineTo(pos2.x, pos2.y);
      context.lineTo(pos3.x, pos3.y);
      context.closePath();
      context.fill();
      context.restore();
    }
  }
  _getCachedStyle(rgb) {
    const key = `${rgb.r},${rgb.g},${rgb.b}`;
    let style = this._colorCache.get(key);
    if (!style) {
      style = getStyleFromRgb(rgb, this._container.hdr);
      this._colorCache.set(key, style);
    }
    return style;
  }
  _getLinkFrequency(p1, p2) {
    return setLinkFrequency([p1, p2], this._freqs.links);
  }
  _getTriangleFrequency(p1, p2, p3) {
    return setLinkFrequency([p1, p2, p3], this._freqs.triangles);
  }
};
export {
  LinkInstance
};
//# sourceMappingURL=LinkInstance-RG36OKF5.js.map
