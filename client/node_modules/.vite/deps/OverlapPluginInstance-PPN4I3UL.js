import "./chunk-LPCC3CIV.js";
import {
  getDistance
} from "./chunk-2FJEDR7Q.js";
import {
  __publicField
} from "./chunk-2TUXWMP5.js";

// node_modules/@tsparticles/interaction-particles-collisions/browser/OverlapPluginInstance.js
var minRetries = 0;
var OverlapPluginInstance = class {
  constructor(container) {
    __publicField(this, "_container");
    __publicField(this, "_hasOverlaps", (particle, pos, tryCount) => {
      const collisionsOptions = particle.options.collisions;
      if (!(collisionsOptions == null ? void 0 : collisionsOptions.enable)) {
        return false;
      }
      const overlapOptions = collisionsOptions.overlap;
      if (overlapOptions.enable) {
        return false;
      }
      const retries = overlapOptions.retries;
      if (retries >= minRetries && tryCount > retries) {
        throw new Error(`Particle is overlapping and can't be placed`);
      }
      return !!this._container.particles.find((p) => getDistance(pos, p.position) < particle.getRadius() + p.getRadius());
    });
    this._container = container;
  }
  checkParticlePosition(particle, position, tryCount) {
    return !this._hasOverlaps(particle, position, tryCount);
  }
};
export {
  OverlapPluginInstance
};
//# sourceMappingURL=OverlapPluginInstance-PPN4I3UL.js.map
